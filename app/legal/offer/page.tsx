import { Placeholder } from "@/components/Placeholder";
export const metadata = { title: "Публичная оферта — TerenLabs" };
export default function Page() {
  return <Placeholder title="Публичная оферта" desc="Оферта на русском и казахском (требование закона РК). Текст готовится с юристом." />;
}
